# PodcastPurifier Docker Compose Environment Variables

# Dockerfile to use: "Dockerfile" (prod, default) or "Dockerfile.dev" (dev with hot reload)
# DOCKERFILE=Dockerfile.dev

# Exposed ports (host ports)
WEB_PORT=8000
REDIS_PORT=6379

# Public hostname for RSS feed URLs (your Tailscale Funnel URL or public domain)
# This is used in the RSS <enclosure> tags to point to your audio files
PUBLIC_HOSTNAME=localhost:8000

# Manager base URL - the URL the Worker uses to upload cleaned audio
# If Worker runs on the same machine as Docker, use localhost
# If Worker runs on a different machine, use the Manager's IP/hostname
MANAGER_BASE_URL=http://localhost:8000

# Frontend password protection (leave empty or unset to disable)
VITE_PASSWORD=
